# my-new-project

카드/전략 기반 웹게임 프로토타입. 로그(전투 기록) 중심으로 턴이 진행되며, 가벼운 웹 스택으로 시작해 추후 2D RPG와 모바일 앱스토어 출시까지 확장하는 것을 목표로 합니다.

## 아키텍처

게임 로직과 UI를 분리해서, 나중에 렌더러만 교체하면 2D RPG로 확장할 수 있도록 설계했습니다.

- `src/engine/` — 순수 TypeScript 함수로 작성된 게임 엔진 (`engine.ts`, `cards.ts`, `races.ts`, `monsters.ts`, `dungeon.ts`, `essence.ts`, `gear.ts`, `stats-calc.ts`, `stat-bonus.ts`, `profile.ts`). 카드 사용, 턴 진행 등 모든 액션이 `GameState`를 새로 계산하고 `log` 배열에 이벤트를 기록합니다. DOM이나 렌더링에 전혀 의존하지 않아 UI를 바꿔도 그대로 재사용 가능합니다.
- `src/ui/` — 화면별 렌더링 (`menu`, `character-select`, `stats`, `dungeon-map`, `inventory`, `equipment`, `essence`, `battle`). `src/main.ts`는 화면 전환만 담당하는 라우터입니다. 나중에 Phaser 기반 2D 렌더러로 교체해도 엔진은 그대로 둘 수 있습니다.

## 시작하기

```bash
npm install
npm run dev       # 개발 서버 실행
npm run build      # 프로덕션 빌드 (dist/)
```

## 현재 구현된 것

- 메인 메뉴에는 "게임 시작" 버튼만 있고, 인벤토리/장비창/정수창은 게임 진입 후(캐릭터 정보 화면, 탐험/전투 화면) 안에서만 접근 가능
- 종족 선택 → 캐릭터 정보(스텟창) → **미궁 입장**
- **미궁 탐험(구역 기반)**: 각 층은 중심부 + 동서남북 4개 구역으로 이루어진 십자 모양 구조
  - 구역마다 테마에 맞는 몬스터가 출몰 (예: 북쪽은 언데드, 동쪽은 야수, 남쪽은 인간형, 서쪽은 화염/마법 계열). 중심부는 4구역의 몬스터가 모두 섞여서 출몰
  - 미궁 입장 시 중심부 또는 그 근처 구역에 랜덤하게 배치되고, 각 구역의 길이(포탈까지 거리)도 매번 새로 정해짐 — 포탈까지 몇 걸음이 필요한지는 입장할 때마다 달라짐
  - 전투가 끝나거나 안전하게 이동한 뒤에는 다음 이동을 선택할 수 있고, 이동할 때마다 확률적으로만 전투가 발생 (매번 싸우지는 않음)
  - 각 구역 끝(동서남북)에는 포탈비석이 있으며, 그 층에서 처음 발견하면 경험치 +2를 즉시 획득. 포탈에 도달하면 "다음 층으로 이동" 버튼으로 층을 올릴 수 있음 (구역/거리 전부 재생성)
  - 층(F)에서 만나는 몬스터의 등급은 F·F+1이 주류(약 80%), F+2가 가끔(약 20%) — 예: 1층은 1~2등급이 주류, 가끔 3등급
- 몬스터는 등급 1~10마다 2종씩, 총 20종. 각 몬스터는 4개 구역 중 하나에 소속되어 있어 같은 구역이라도 매번 다른 몬스터를 만날 수 있음
- 종족별 고정 스텟(체력/마나/공격/방어 보너스): 인간, 엘프, 오크, 드워프
- 플레이어 vs 몬스터 턴제 카드 배틀 (공격/방어/회복 카드), 마나 기반 카드 사용, 자동 적 AI 턴
- 몬스터 등급(1~10)에 따른 경험치 획득, 같은 이름의 몬스터는 최초 처치 시에만 경험치 지급
- 레벨/경험치는 `localStorage`에 저장되어 브라우저를 다시 열어도 유지됨, 스텟창에 표시
- **정수(essence) — 흡수형 장착**: 몬스터 처치 시 0.1% 확률로 그 몬스터 고유의 정수가 드랍. 그 자리에서 흡수할지 버릴지만 선택 가능 (별도 보관 없음). 흡수하면 레벨당 1개씩 열리는 슬롯에 순서대로 영구 장착됨 (해제 UI 없음 — 특수한 방법으로만 해제 가능). 각 정수는 드랍된 몬스터의 특성을 반영한 고정 스텟 보너스 + 전용 스킬 카드를 부여
- **장비(무기/방어구/장신구) — 몸에 착용하는 일반 장비**: 정수와 완전히 별개 시스템. 몬스터 처치 시 3% 확률로 드랍되어 인벤토리에 자동으로 담기고, 장비창에서 자유롭게 장착·해제 가능 (해제하면 다시 인벤토리로)
- 장착한 정수와 장비의 스텟 보너스는 모두 종족 스텟에 합산되고, 정수 스킬은 전투 덱에 추가됨
- **화면 3분리**:
  - `인벤토리` — 마석(처치 시 5% 확률로 드랍, 자동 보관), 미착용 장비, 금화 표시(금화는 드랍되지 않고 표시만)
  - `장비창` — 무기/방어구/장신구 슬롯. 자유롭게 장착·해제
  - `정수 창` — 지금 장착 중인 정수 목록 + 지금까지 발견한 정수 도감(흡수 안 해도 발견 기록은 남음)
- 모든 행동이 로그로 기록되고 화면에 누적 표시

## 로드맵

- [ ] 2D RPG로 확장: `src/engine`은 유지하고 Phaser 등으로 렌더러 교체, 맵/이동/전투 씬 추가
- [ ] 모바일 앱스토어 출시: [Capacitor](https://capacitorjs.com/)로 `dist/` 빌드를 iOS/Android 네이티브 셸에 패키징 (`npx cap init`, `npx cap add ios/android`)
- [ ] 카드 풀 확장, 덱 빌딩, 저장/불러오기
- [ ] 정수 해제(특수 장치) 시스템
- [ ] 장비 종류 확장, 금화 획득 수단(상점/보상 등) 설계
- [ ] 미궁 층별 보스/이벤트, 도달한 최고 층수 기록
- [ ] 서쪽 구역처럼 저층 몬스터가 없는 구역의 난이도 밸런싱 (현재는 가장 가까운 등급으로 대체)

## 라이선스

이 프로젝트의 라이선스를 여기에 명시하세요.
